package com.pluralsight;

public class Program {
    public static void main(String[] args) {
        // Create the UserInterface object
        UserInterface ui = new UserInterface();

        // Run the program (start the menu)
        ui.display();
    }
}
